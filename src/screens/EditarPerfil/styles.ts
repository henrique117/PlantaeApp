import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    texto: {
        textAlign: 'center',
        fontWeight: 'bold',
        fontSize: 20
    },

    voltar: {
        paddingLeft: 10,
        paddingTop: 12
    },
});

export default styles;